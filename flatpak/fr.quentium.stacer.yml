id: fr.quentium.stacer
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
command: stacer
finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  # Need to access user autostart for Startup Apps tab to be useful
  - --filesystem=xdg-config/autostart

modules:
  - name: stacer
    buildsystem: cmake-ninja
    sources:
      - type: dir
        path: ..
